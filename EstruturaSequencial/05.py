# Faça um sistema que converta metros para centimetro
metros = eval(input('Digite o valor em Metros\n'))
def converte():
    centimetros = (metros * metros) * 100
    print(f'O valor de {metros} metros serão {centimetros} centimentros')

convertido = converte()
